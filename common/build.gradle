architectury { common("fabric", "forge") }
loom { mixin { defaultRefmapName = "immersivesnow.refmap.json" } }

dependencies {
    modImplementation "net.fabricmc:fabric-loader:${rootProject.fabric}"
}
